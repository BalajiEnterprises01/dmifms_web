NEXT_PUBLIC_SITE_EMAIL=dm23@dm23.co.in
NEXT_PUBLIC_SITE_PHONE=+91 9686441441

# Admin login (set real values in production; do not ship the code fallbacks)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
ADMIN_SECRET=change-me

# SMTP: contact form enquiry emails. If unset, leads are still saved to data/leads.json.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=dm23@dm23.co.in

# Webhook that appends each lead as a row in the Google Sheet.
# Works with any catch-style webhook, currently a Make.com "Custom webhook"
# (free tier; Zapier charges for webhook triggers).
# If unset, leads are still saved and emailed; only the sheet row is skipped.
SHEET_WEBHOOK_URL=
NEXT_PUBLIC_LINKEDIN_URL=#
NEXT_PUBLIC_X_URL=#
NEXT_PUBLIC_FACEBOOK_URL=#


